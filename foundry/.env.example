# Microsoft Foundry project endpoint (Build → Overview → Project endpoint)
# Example: https://<resource>.services.ai.azure.com/api/projects/<project>
PROJECT_CONNECTION_STRING=

# Deployment that EXISTS in this Foundry project (Models + endpoints).
# Gemini first for Paso 3 — deploy gemini-2.5-flash (or your Gemini name) in Foundry.
# Override later with Claude if needed: MODEL_DEPLOYMENT_NAME=claude-sonnet-4-5
MODEL_DEPLOYMENT_NAME=gemini-2.5-flash
# Force republish agents/workflow on next Chat turn (picks up new model)
FOUNDRY_REFRESH_AGENTS=true

# Agent names (visible in Foundry → Build → Agents)
IGNITE_DOCUMENT_AGENT=ignite-document-agent
# Primary media/image agent tab in Foundry (also publishes ignite-media-agent alias)
IGNITE_MEDIA_AGENT=ignite-image-agent
IGNITE_MEDIA_AGENT_ALIAS=ignite-media-agent
IGNITE_ORCHESTRATOR_AGENT=ignite-orchestrator-agent
IGNITE_WORKFLOW_AGENT=ignite-document-workflow

# Ignite Chat (app/) → GenAI Traces → auto ensure agents+workflow → orchestrate
# Manual python agents.py / workflow.py optional; Chat does this on each turn.
FOUNDRY_ORCHESTRATION_ENABLED=true
FOUNDRY_BRAIN_LIVE=false
FOUNDRY_WORKFLOW_LIVE=false

# Challenge 2 — tracing (set before running monitor.py)
AZURE_EXPERIMENTAL_ENABLE_GENAI_TRACING=true
APPLICATIONINSIGHTS_CONNECTION_STRING=

# Align inspect_document with Ignite API extract (optional lab only; CI keeps false)
FOUNDRY_USE_IGNITE_API=false
# IGNITE_EXTRACTION_API_URL=https://ignite-api.azurecontainerapps.io
# IGNITE_EXTRACTION_API_KEY=
# IGNITE_API_TIMEOUT=300
